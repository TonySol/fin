"""The package includes modules that describe methods to work with department and employee services:

– `services` holds classes with methods to fetch employees and department data from DB
– `validation` holds support class to validate user inputs before fetching data from DB
"""
